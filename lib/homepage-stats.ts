/**
 * Public “About” stats on the homepage. Kept in sync with `prisma/seed.ts` (logged on seed).
 */
export const HOMEPAGE_ABOUT_STATS = {
  albums: 2,
  yearsMinistry: 20,
  yearsMinistrySuffix: '+',
  continents: 3,
} as const
